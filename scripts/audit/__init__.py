"""The owner's after-the-fact look: the audit sheet (`sheet.py`) built from
the rows every terminal rung appends, and the note (`note.py`) that turns a
finding into a casebook row.  No refs or episode step imports this package or
reads what it reads."""
