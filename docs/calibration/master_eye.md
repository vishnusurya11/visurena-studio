# master_eye — the master rubric from measures

## First contact 2026-09-25 — one accepted, published episode, 30 frames

Run with the live tools over a finished master (shadow, faces, board, repeats,
story, a 30-frame read, cross-take identity). 1051 s wall, most of it queued
behind another session's render on the shared GPU.

| field | read | ruling |
|---|---|---|
| faces | medium closes 0.145, 0.187, 0.188, 0.216, 0.227; closes 0.198, 0.426 (share of frame height) | the 0.25 floor was a proposal, not a measure; now 0.12 for a medium close, 0.18 for a close; `fitted_on` = this episode, n=7 |
| story | the motion clause's every content word was a "verb" (toward, small, amplitude…); the reader listed none, checking, lying | verbs are now the ACTING verbs of the turn shot's prose, camera verbs excluded; no acting verb = cannot tell; a miss is a low-severity flag, never a refusal |
| identity | one of six reads of the narrator at 0.658 against DRIFT 0.75, the rest above | a drift needs at least two reads and a quarter of them under the wall; one hard-lit frame is not a drift |
| shadow, board, repeats | y | — |

Take judge on the same episode: 23 takes, 46 reads, pass, 57 s. Panel judge:
died on a 120 s ask timeout behind the other session's render; timeout now 900 s
and a dead ask is a missing measure for that panel.

