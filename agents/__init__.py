"""Studio agents — one file per agent. Each declares TIER + SKILL + schema + entry fn.

Models come from models.yaml via studio.llm (the tier system); agents never name a
provider or model directly.
"""
